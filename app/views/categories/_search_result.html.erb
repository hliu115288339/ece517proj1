<% if @categories.empty? %>
    No posts
<% else %>
<% @categories.each do |category| %>
    <% @category = category %>
    <h4><%= link_to @category.name, @category %></h4>
    <%= render 'categories/list_posts' %>
<% end %>
<% end %>