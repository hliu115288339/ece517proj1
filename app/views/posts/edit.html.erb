<% provide(:title, "Edit Post") %>

<% if @post.parent_post_id.nil? %>
    <h1>Editing Post</h1>
    <%= render 'post_form' %>
<% else %>
    <h1><%= "Editing #{@post.title}" %></h1>
    <%= render 'comment_form' %>
<% end %>


